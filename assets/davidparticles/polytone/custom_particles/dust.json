{
  "initializer":{
    "lifetime": "24+rand()*18",
    "size": "4.25",
    "alpha": "0",
    "roll": "rand() * 2 * pi",
    "custom": "rand()"
  },
      "ticker": {
       "alpha": "0.65 * sin(((pi/LIFETIME)) * AGE) * max(0, min(1, (15 - sqrt(DISTANCE_SQUARED)) / 5))",
            "dx": "(0.25 + RAIN * 0.05) + CUSTOM * 0.1 * sin(TIME * 0.2 + CUSTOM * 10)",
            "dy": "(0.25 * cos(0.05 * TIME) - RAIN * 0.1)*0.5",
            "dz": "0.05 * sin(0.01 * TIME + CUSTOM * 8)",
            "roll": "ROLL",
            "remove_condition": "sqrt(DISTANCE_SQUARED) > 15"
        },
  "render_type": "additive_translucent",
  "liquid_affinity": "any",
  "exclusion_radius": 3,
  "has_physics": true,
  "kill_on_contact": true,
  "offset": [0, 0, 0],
  "colormap": "davidparticles:colormap_sandstorm"
}